{{#each this}}
  <div class="row">
    <div class="col-sm-6">
      <div class="card">
        <div class="card-body">
          <h5 class="card-text">{{commentText}}</h5>
          <p class="card-title">Commented on {{commentDateFormat (dateNow) format='MMMM Do YYYY'}}</p>
        </div>
      </div>
    </div>
  </div>
{{/each}}
