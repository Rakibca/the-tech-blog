<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      <p>Commented by {{name}} on {{format_date post.date_created}}</p>
    </div>
    <div class="text">
      {{commentText}}
      <p></p>
    </div>
  </section>
  {{/each}}
</div>
